import 'package:flutter/material.dart';

class Leetcode_past extends StatelessWidget {
  const Leetcode_past({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
